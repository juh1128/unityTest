using UnityEngine;
using System.Collections;

static public class globalSetup{

    static public float widthRatio = (float)Screen.width / 360;
    static public float heightRatio = (float)Screen.height / 640;
    static public int footerIndex = 0;
}
